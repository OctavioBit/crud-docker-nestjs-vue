// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:227733436mysql@172.28.1.1:3306/kamaebackdb?schema=public&allowPublicKeyRetrieval=true"
}

model Dogs {
  id              Int             @id @default(autoincrement()) @map("ID_DOG")  
  name          String          @map("NAME")
  sex            String          @map("SEX")
  sterilized    Boolean         @map("STERILIZED")
  birthdate DateTime        @map("BIRTHDATE")

  @@map("TBL_DOGS")
}